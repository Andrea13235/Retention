node_modules/
.env*
!.env.example
*.tsbuildinfo
.DS_Store
dist/
build/

# Spec interno di lavorazione — NON pubblicare (resta solo in locale)
andrea-video-skill.md

# test artifacts
.tmp-e2e/
tmp-dbg/

# Local agent workspace (attachments, internal notes) — never publish
.hermes/
